spring:
  application:
    name: nacos-config
#  profiles:
#    active: dev
  cloud:
    nacos:
      discovery:
        # 注册中心
        server-addr: '192.168.1.104:8848'
        username: nacos
        password: nacos
      config:
        # 配置中心
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        # 配置文件格式 properties 或者 yaml
        file-extension: properties
        # 命名空间的id
        namespace: '76daba5d-d72d-47b8-a004-a3f3c9aea4ce'
        group: DEFAULT_GROUP

